apiVersion: v1
kind: Service
metadata:
  name: modelpulse-service
spec:
  type: NodePort
  selector:
    app: modelpulse
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30007